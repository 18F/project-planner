name: Kanban Entity
description: Creates a configuration entity displaying a workflow Kanban board of nodes in configurable swimlanes.
package: Custom

type: module
core: 8.x

dependencies:
  - drupal:block
  - drupal:views
  - drupal:workflow
